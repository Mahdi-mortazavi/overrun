# OVERRUN — نقشه‌ی کامل پروژه
### از یک فایل HTML تک‌نفره تا یک شوترِ آنلاینِ چندنفره با توزیع APK + PWA

**تاریخ:** ۱۴۰۵/۰۵/۰۹ (۳۱ جولای ۲۰۲۶)
**وضعیت:** پلن — هنوز هیچ کدی اجرا نشده. منتظر تایید شما.

---

## ۱. وضعیت فعلی (چیزی که الان دارید)

فایل `E:\game shooter\game shooter.html` را کامل خواندم. خلاصه‌ی صادقانه‌اش:

| مورد | وضعیت |
|---|---|
| حجم | ۱۲۵ کیلوبایت، ۲۷۱۶ خط، تک‌فایل |
| موتور | three.js 0.185 از CDN، بدون build step |
| دارایی‌ها | **صفر بایت** — همه‌چیز پروسیجرال (بافت‌ها با Canvas، صداها با WebAudio، مدل‌ها با هندسه‌ی اولیه) |
| معماری | تمیز و ماژولار: `T` (تیونینگ متمرکز)، Bus، Audio، World، Hash (spatial hash)، Feel، Input، VFX، Player، Enemies، Projectiles، Pickups، Turrets، Abilities، Upgrades، Director، UI، Game |
| حلقه | شبیه‌سازی ثابت ۶۰Hz با accumulator + رندر با interpolation + hitstop — **این خیلی خوب است و پایه‌ی درستی برای نت‌کد است** |
| دشمنان | ۷ آرکتایپ (rusher/bruiser/spitter/splitter/shard/elite/boss) روی یک هسته‌ی AI: seek + separation + arrival |
| سلاح‌ها | ۴ تا (SMG، BREACHER، LANCE، RICOCHET) |
| دوربین | تاپ‌داون ثابت، ارتفاع ۲۷، FOV ۵۲، با density pull-back |
| نور | فضای تیره (`#05080C`)، مه نمایی چگال، پالت "petrol/amber/rose" |
| مولتی‌پلیر | **هیچ** |
| PWA | فقط چند متاتگ — نه manifest، نه service worker |

**نکته‌ی مثبت مهم:** کد به‌شکل غیرمعمولی تمیز است. حلقه‌ی fixed-timestep و spatial hash دقیقاً همان چیزی است که برای اضافه‌کردن نت‌کد authoritative لازم است. مجبور نیستیم از صفر بنویسیم — بازنویسیِ ساختاریافته انجام می‌دهیم.

**ضعف‌های کلیدی که باید حل شوند:** محیط تیره و یکنواخت، دشمنان بی‌هوش (فقط دنبال بازیکن می‌دوند)، بدون انیمیشن اسکلتی، صداهای محدود، تک‌فایل غیرقابل نگهداری، صفر زیرساخت شبکه.

---

## ۲. تصمیم‌های قفل‌شده (بر اساس انتخاب‌های شما)

| موضوع | تصمیم |
|---|---|
| **نت‌کد** | هیبرید — سرور معتبر روی **Cloudflare Durable Object** + **Supabase** برای auth/لابی/آمار |
| **سبک بصری** | روز روشن / آرنای بتنی-فلزی، آسمان HDR، سایه‌های نرم |
| **دارایی‌ها** | هیبرید — مدل‌های GLTF سبک CC0 + انیمیشن اسکلتی + ۳۰-۴۰ صدای واقعی فشرده (~۸-۱۵MB روی R2) |
| **APK** | Capacitor + بیلد و امضای خودکار در GitHub Actions |
| **مودها** | TDM ۴v۴ / Squad ۲v۲v۲v۲ / Co-op موجی ۴ نفره — حداکثر ۸ بازیکن |
| **Supabase** | اسکیمای `game` در پروژه‌ی موجود `Mahdi-mortazavi's Project` (irkxujavsreiwmoppwhh) |
| **دامنه** | زیردامنه‌ی رایگان `*.workers.dev` (بعداً قابل ارتقا) |
| **تحویل** | خودکار تا آخر — همه‌ی مراحل پشت سر هم |

---

## ۳. معماری هدف

```
                          ┌─────────────────────────────┐
                          │   Cloudflare Worker (edge)  │
   بازیکن ────HTTPS───────►  • سرو کردن PWA و لندینگ    │
   (PWA/APK)              │  • API روم‌ها و مچ‌میکینگ    │
       │                  └──────────┬──────────────────┘
       │                             │
       │                  ┌──────────▼──────────────────┐
       └────WebSocket─────►  Durable Object: MatchRoom  │
                          │  • یک DO به ازای هر مسابقه  │
                          │  • تیک ۲۰Hz، سرور معتبر     │
                          │  • hit-registration سروری   │
                          │  • lag-compensation ۲۰۰ms   │
                          │  • SQLite برای وضعیت روم    │
                          └──────────┬──────────────────┘
                                     │ (پایان مسابقه)
                          ┌──────────▼──────────────────┐
                          │   Supabase (eu-west-1)      │
                          │  • Auth (مهمان + ایمیل)     │
                          │  • schema `game`:           │
                          │    profiles, matches,       │
                          │    match_players, rooms,     │
                          │    leaderboard, unlocks     │
                          │  • Realtime: لابی، Presence، │
                          │    چت، دعوت با کد            │
                          │  • RLS روی همه‌ی جدول‌ها     │
                          └─────────────────────────────┘

   R2 bucket: مدل‌های GLTF (Draco/Meshopt)، صداها (opus/webm)، اسکرین‌شات‌ها
```

**تقسیم کار بین Supabase و Cloudflare — چرا این‌طور:**

- **Supabase Realtime** برای هر چیزی که کند و باحالت است: لابی، لیست دوستان، Presence («۳ نفر در روم منتظرند»)، چت، دعوت با کد. اینجا تاخیر ۲۰۰ms مشکلی نیست و Postgres + RLS دقیقاً ابزار درست است.
- **Durable Object** برای خودِ مسابقه: تیک ۲۰Hz، موقعیت‌ها، تیرها، ثبت برخورد. Supabase Realtime برای این کار ساخته نشده — هر پیام از client → سرور Supabase → دیتابیس → پخش می‌رود و برای شوتر تاخیرش کشنده است. DO یک پروسه‌ی درجاست با ۰ هاپ اضافه.

---

## ۴. ساختار مخزن (GitHub: `Mahdi-mortazavi/overrun`)

```
overrun/
├─ packages/
│  ├─ shared/              # کدِ مشترک client+server (منبع واحد حقیقت)
│  │  ├─ constants.js      # T — همان تیونینگ فعلی، توسعه‌یافته
│  │  ├─ sim.js            # فیزیک و حل برخورد، deterministic
│  │  ├─ protocol.js       # سریالایز باینری پیام‌ها
│  │  └─ modes.js          # قوانین TDM / Squad / Co-op
│  ├─ client/
│  │  ├─ src/
│  │  │  ├─ render/        # صحنه، نور، پس‌پردازش، LOD، instancing
│  │  │  ├─ entities/      # بازیکن، دشمن، پرتابه، pickup
│  │  │  ├─ anim/          # کنترلر انیمیشن اسکلتی + blend
│  │  │  ├─ audio/         # میکسر، bus ها، صداهای فضایی
│  │  │  ├─ net/           # کلاینت WS، prediction، reconciliation، interpolation
│  │  │  ├─ ui/            # HUD، منو، لابی، صفحه‌ی روم
│  │  │  └─ ai/            # AI دشمن (کلاینت در تک‌نفره، سرور در آنلاین)
│  │  └─ public/           # manifest، آیکون‌ها، service worker
│  ├─ server/              # Cloudflare Worker + Durable Objects
│  │  ├─ index.js          # روتر: static، API، ارتقا به WS
│  │  ├─ MatchRoom.js      # Durable Object — حلقه‌ی معتبر بازی
│  │  ├─ Matchmaker.js     # Durable Object — صف رندوم
│  │  └─ wrangler.toml
│  ├─ landing/             # لندینگ پیج
│  └─ android/             # پروژه‌ی Capacitor
├─ tools/
│  ├─ asset-pipeline/      # فشرده‌سازی GLTF (Draco) و صدا (opus)
│  └─ screenshot/          # گرفتن اسکرین‌شات خودکار با Playwright
└─ .github/workflows/
   ├─ deploy.yml           # دیپلوی Worker + لندینگ
   └─ android.yml          # بیلد و امضای APK → Releases
```

**نکته:** بازی همچنان بدون build step پیچیده کار می‌کند — از Vite با خروجی تک‌باندلِ کوچک استفاده می‌کنم، نه یک زنجیره‌ی سنگین.

---

## ۵. مراحل اجرا (۱۰ مایل‌ستون)

### M1 — پایه‌گذاری و بازسازی
- ساخت مخزن `overrun` روی GitHub، تبدیل تک‌فایل به ساختار ماژولار بالا
- استخراج `shared/` طوری که همان کد فیزیک روی کلاینت و سرور اجرا شود
- راه‌اندازی Vite، ESLint، و اسکریپت dev
- **خروجی:** بازی فعلی دقیقاً مثل قبل کار می‌کند ولی روی معماری جدید

### M2 — بازطراحی بصری: محیط روشن
- **نور:** آسمان فیزیکی (Sky shader)، خورشید جهت‌دار با سایه‌ی CSM، نور محیطی از HDR، تن‌مپینگ ACES با نمایش بهتر
- **آرنا:** بتن روشن + عناصر فلزی، جزئیات پروسیجرال (خط‌کشی، لکه، زنگ)، مه ارتفاعی رقیق به‌جای مه چگال
- **پس‌پردازش:** Bloom انتخابی، SSAO سبک، color grading با LUT، chromatic aberration فقط هنگام ضربه، vignette پویا
- **کیفیت تطبیقی:** ۴ سطح کیفیت که خودکار بر اساس FPS انتخاب می‌شوند (سایه، SSAO، ذرات، رزولوشن رندر)
- **خروجی:** آرنای روشن و خوانا با ۶۰fps پایدار روی موبایل میان‌رده

### M3 — دارایی‌ها، انیمیشن و صدا
- انتخاب و بهینه‌سازی مدل‌های CC0 (Quaternius / Kenney / Poly Pizza) — بازیکن + ۵ دشمن + سلاح‌ها
- فشرده‌سازی با Draco + Meshopt + KTX2 برای بافت‌ها
- سیستم انیمیشن: idle / run / strafe / dash / shoot / hit / death با blend نرم و IK ساده برای پا و هدف‌گیری
- افکت‌های procedural مکمل: squash & stretch، recoil، ragdoll ساده هنگام مرگ
- کتابخانه‌ی صوتی: ۳۵-۴۰ صدا (سلاح‌ها با لایه‌بندی، برخورد، پا، صدای دشمنان، UI، موسیقی تطبیقی که با شدت بازی لایه اضافه می‌کند)
- صدای فضایی سه‌بعدی (PannerNode) + ducking هوشمند + میکسر با bus
- **خروجی:** بازی حس AAA می‌دهد، حجم دانلود اولیه زیر ۱۵MB با لود تدریجی

### M4 — دوربین، کنترل و UX
- دوربین جدید: تاپ‌داون مایل با کنترل زاویه، shoulder-offset، aim-lead هوشمند، shake با ضربه‌ی جهت‌دار، dolly-zoom در لحظات حساس
- کنترل موبایل: جوی‌استیک شناور دوگانه با dead-zone تطبیقی، auto-aim کمکی قابل تنظیم، دکمه‌های بزرگ‌تر با هیت‌باکس گشاد
- کنترل PC: WASD + ماوس + کیبورد کامل + پشتیبانی Gamepad
- HUD بازطراحی‌شده برای چندنفره: نوار سلامتی تیم، نشانگر جهت دشمن، kill feed، مینی‌مپ
- هپتیک: الگوهای متفاوت برای ضربه‌زدن/ضربه‌خوردن/dash/kill
- **خروجی:** UX در سطح بازی‌های تجاری موبایل

### M5 — AI پیشرفته‌ی دشمنان
جایگزینی «همه دنبال بازیکن می‌دوند» با:
- **Utility AI**: هر دشمن امتیاز چند رفتار (نزدیک‌شدن، محاصره، عقب‌نشینی، پوشش‌گرفتن، هماهنگی) را می‌سنجد و بهترین را انتخاب می‌کند
- **حافظه و ادراک:** خط دید، آخرین موقعیت دیده‌شده، جست‌وجوی هوشمند به‌جای دانستن جادویی موقعیت بازیکن
- **هوش گروهی:** یک «فرمانده» مجازی نقش‌ها را پخش می‌کند — بعضی محاصره می‌کنند، بعضی فشار می‌آورند، بعضی مسیر فرار را می‌بندند
- **Flow-field pathfinding** روی گرید آرنا: مسیریابی بدون گیرکردن پشت موانع، با هزینه‌ی محاسباتی ثابت حتی با ۲۰۰ دشمن
- **تله‌گراف بهتر:** هر حمله ≥۲۵۰ms هشدار بصری+صوتی می‌دهد (منصفانه ولی سخت)
- **Director هوشمندتر:** تحلیل عملکرد بازیکن (دقت، حرکت، فاصله) و ترکیب موج بر همان اساس — اگر بازیکن خوب dash می‌کند، دشمنان دورزننده بیشتر می‌آیند
- **خروجی:** دشمنان باهوش، غیرقابل‌پیش‌بینی، اما همیشه منصفانه

### M6 — بک‌اند: Supabase
- ساخت اسکیمای `game` با جدول‌ها: `profiles`، `rooms`، `room_members`، `matches`، `match_players`، `leaderboards`، `unlocks`، `friendships`
- RLS کامل روی همه‌ی جدول‌ها
- Auth: ورود مهمان (anonymous) برای شروع فوری + ارتقا به ایمیل/OTP
- Realtime: کانال لابی، Presence، چت روم، رویداد دعوت
- Edge Function برای ثبت امن نتیجه‌ی مسابقه (فقط از سمت DO با service key)
- **خروجی:** بک‌اند حساب‌ها، لابی و آمار کار می‌کند

### M7 — نت‌کد: Durable Objects
- `MatchRoom` DO: حلقه‌ی ۲۰Hz، دریافت ورودی بازیکنان، اجرای همان `shared/sim.js`، پخش snapshot دلتا-فشرده به‌صورت باینری
- کلاینت: **client-side prediction** برای حرکت خود بازیکن، **server reconciliation** با rollback، **entity interpolation** برای بقیه، **lag compensation** تا ۲۰۰ms برای ثبت شات
- `Matchmaker` DO: صف رندوم بر اساس مود، تشکیل روم و هدایت بازیکنان
- روم خصوصی: کد ۶ حرفی، لینک دعوت `overrun.workers.dev/j/ABC123`، دعوت مستقیم از لیست دوستان
- مدیریت قطعی: reconnect با grace period، پرکردن جای خالی با بات، پایان تمیز مسابقه
- **ضدتقلب:** همه‌چیز سروری — سرعت، نرخ شلیک، آسیب، خط دید. کلاینت هیچ‌وقت به سرور نمی‌گوید «من زدم».
- **خروجی:** مسابقه‌ی ۸ نفره‌ی روان با پینگ واقعی از ایران

### M8 — مودهای بازی و طراحی دوپامین
- **TDM ۴v۴:** اولین تیم به ۳۰ کیل، آرنای متقارن، respawn ۴ ثانیه‌ای با سپر کوتاه
- **Squad ۲v۲v۲v۲:** ۴ تیم دونفره، امتیاز بر اساس کیل، دور آخر «مرگ ناگهانی» با آرنای کوچک‌شونده
- **Co-op موجی ۴ نفره:** همان بازی فعلی ولی تیمی، با احیای هم‌تیمی، باس مشترک و ارتقاهای رأی‌گیری‌شده
- **حلقه‌ی دوپامین:** پیشرفت قابل‌مشاهده (XP، سطح، آنلاک سلاح/اسکین)، کیل‌استریک با اعلام صوتی، near-miss ها که تقریباً همیشه قابل برگشت‌اند، پاداش متغیر (جعبه‌های تصادفی در آرنا)، آمار پایان بازی با highlight شخصی، «یک دست دیگر» با شروع مجدد در زیر ۳ ثانیه
- **خروجی:** سه مود متعادل و اعتیادآور

### M9 — PWA، APK و توزیع
- **PWA حرفه‌ای:** manifest کامل، آیکون maskable، `display: fullscreen`، قفل orientation به landscape، Service Worker با precache کامل (بازی کامل آفلاین قابل اجرا)، Wake Lock، Fullscreen API، نصب‌پذیر روی iOS/Windows/Mac
- **iOS خاص:** رفع محدودیت‌های Safari (AudioContext با اولین لمس، safe-area، جلوگیری از bounce، آیکون‌های apple-touch)
- **APK:** پروژه‌ی Capacitor با WebView مدرن، اسپلش‌اسکرین، آیکون تطبیقی، immersive fullscreen، هپتیک نیتیو، مدیریت دکمه‌ی back، دارایی‌ها bundle شده برای اجرای آفلاین
- **GitHub Actions:** بیلد + امضا با keystore ذخیره‌شده در Secrets → آپلود به Releases → لینک دانلود پایدار
- **خروجی:** `OVERRUN.apk` امضاشده + PWA نصب‌پذیر

### M10 — لندینگ پیج و انتشار
- صفحه‌ی فرود روی Cloudflare Worker: hero با ویدیوی گیم‌پلی، توضیح بازی، گالری اسکرین‌شات (خودکار از خود بازی گرفته می‌شود با Playwright)، معرفی مودها، دکمه‌ی «بازی در مرورگر»، دکمه‌ی دانلود APK، راهنمای نصب PWA برای iOS/Android/Windows/Mac
- دوزبانه (فارسی + انگلیسی)، RTL درست، سریع (زیر ۱۰۰KB، LCP < 1s)
- SEO، Open Graph، آیکون‌ها، فاوآیکون
- تست نهایی: مچ واقعی ۴ نفره، پروفایل عملکرد روی موبایل، چک‌لیست کیفیت
- **خروجی:** محصول منتشرشده و قابل معرفی

---

## ۶. اعداد و محدودیت‌های واقعی

**Cloudflare رایگان (تایید شده از داکیومنت):**
- Durable Objects روی پلن رایگان **در دسترس است** (فقط با بک‌اند SQLite)
- سقف: ۱۰۰٬۰۰۰ درخواست/روز، ۱۳٬۰۰۰ GB-s/روز
- پیام‌های ورودی WebSocket با نسبت ۲۰:۱ حساب می‌شوند
- **محاسبه:** مسابقه‌ی ۸ نفره با ورودی ۲۰Hz ≈ ۱۶۰ پیام/ثانیه ≈ ۸ درخواست حساب‌شده/ثانیه ≈ ۴۸۰/دقیقه
- **یعنی روی پلن رایگان: حدود ۳ ساعت مسابقه‌ی ۸ نفره در روز.** برای تست و بازی با دوستان کافی است. برای انتشار عمومی جدی، پلن ۵ دلاری Workers لازم می‌شود (که سقف را ~۳۰ برابر می‌کند).

**Supabase رایگان:** ۵۰۰MB دیتابیس، ۲۰۰ اتصال همزمان Realtime، ۲ میلیون پیام/ماه — برای لابی و آمار فراوان است.

**بودجه‌ی عملکرد (هدف):**
| دستگاه | هدف |
|---|---|
| موبایل میان‌رده (Snapdragon 6xx) | ۶۰fps پایدار، افت زیر ۵٪ |
| موبایل ضعیف | ۳۰fps با کیفیت خودکار پایین |
| PC | ۱۲۰fps+ |
| حجم دانلود اولیه PWA | زیر ۱۵MB |
| زمان تا اولین بازی | زیر ۴ ثانیه روی 4G |
| حجم APK | زیر ۳۵MB |

---

## ۷. ریسک‌ها — صادقانه

1. **دسترسی از ایران.** `*.workers.dev` و `*.supabase.co` گاهی از داخل ایران فیلتر یا کند می‌شوند. این بزرگ‌ترین ریسک محصول است، نه ریسک فنی. راهکار: بعد از راه‌اندازی، با دامنه‌ی اختصاصی روی همان Cloudflare تست می‌کنیم (معمولاً بهتر جواب می‌دهد). اگر باز هم مشکل بود، APK آفلاین + مود تک‌نفره همیشه کار می‌کند و فقط بخش آنلاین محدود می‌شود.

2. **کیستور امضای APK.** برای امضای APK یک keystore می‌سازم و در GitHub Secrets می‌گذارم. **این فایل و رمزش را باید شما جای امنی نگه دارید** — اگر گم شود، دیگر نمی‌توانید نسخه‌ی به‌روز همان اپ را منتشر کنید. بعد از ساخت، آن را برایتان می‌فرستم.

3. **بیلد اندروید در محیط ابری.** بیلد APK نیاز به Android SDK + Gradle دارد. آن را در GitHub Actions انجام می‌دهم (نه در این کانتینر) که پایدارتر و تکرارپذیر است.

4. **نت‌کد سخت‌ترین بخش است.** prediction/reconciliation درست کار سنگینی است. اگر جایی به مشکل خوردم، ابتدا نسخه‌ی ساده‌تر (interpolation کامل، بدون prediction) را می‌سازم که کار کند، بعد بهینه‌اش می‌کنم. بازی هیچ‌وقت در حالت خراب رها نمی‌شود.

5. **مقیاس پروژه.** این عملاً ساختِ یک بازی تجاری است. کار طولانی خواهد بود و در چند نوبت انجام می‌شود. بعد از هر مایل‌ستون کامیت می‌کنم تا همیشه یک نسخه‌ی سالم روی GitHub داشته باشید.

---

## ۸. چیزهایی که از شما لازم دارم

| مورد | وضعیت |
|---|---|
| دسترسی GitHub | ✅ متصل (`Mahdi-mortazavi`) |
| دسترسی Cloudflare | ✅ متصل (اکانت فعال، یک Worker موجود) |
| دسترسی Supabase | ✅ متصل (پروژه‌ی `irkxujavsreiwmoppwhh`) |
| نام مخزن | پیشنهاد من: `overrun` — اگر نام دیگری می‌خواهید بگویید |
| نام بازی | فعلاً **OVERRUN** (نام فعلی). اگر نام فارسی/دیگری می‌خواهید بگویید |
| کیستور اندروید | من می‌سازم، به شما تحویل می‌دهم |

**سوال باز (می‌توانم خودم تصمیم بگیرم اگر نظری ندارید):**
- ورود به بازی: پیشنهاد من ورود مهمان با یک کلیک است (بدون ثبت‌نام) و ارتقا به ایمیل اختیاری. اگر می‌خواهید ثبت‌نام اجباری باشد بگویید.
- زبان رابط بازی: پیشنهاد من انگلیسی برای HUD (کوتاه و استاندارد) + لندینگ پیج دوزبانه. اگر HUD فارسی می‌خواهید بگویید.

---

## ۹. قدم بعدی

با تایید شما از M1 شروع می‌کنم و تا M10 پشت سر هم پیش می‌روم (طبق انتخاب «خودکار تا آخر برو»). بعد از هر مایل‌ستون یک کامیت روی GitHub و یک پیام کوتاه از پیشرفت می‌گیرید، و نسخه‌های قابل تست را همان‌جا تحویل می‌دهم.

اگر می‌خواهید ترتیب مراحل عوض شود (مثلاً اول مولتی‌پلیر بعد گرافیک) الان بگویید.
