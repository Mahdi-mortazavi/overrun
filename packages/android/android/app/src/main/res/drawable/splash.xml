<?xml version="1.0" encoding="utf-8"?>
<!-- Splash: the game's background colour with the mark centred at its natural
     size. A layer-list is used rather than a full-bleed PNG so the logo is never
     stretched to the aspect ratio of whatever phone this lands on. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/splashBackground" />
    <item>
        <bitmap
            android:src="@drawable/splash_logo"
            android:gravity="center"
            android:tileMode="disabled" />
    </item>
</layer-list>
